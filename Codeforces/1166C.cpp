#include<bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
    int n;
    vector<ll> arr;

    cin >> n;
    arr.assign(n,0);
    for(int i=0;i<n;i++){
        cin >> arr;
    }
    sort(arr.begin(),arr.end());
    int l = 0, r = 1, answ = 0;
    while(){

    }

    return 0;
}