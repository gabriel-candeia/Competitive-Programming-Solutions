#include<bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
    ll a, b, c, d;
    vector<int> arr;

    cin >> a >> b >> c >> d;
    for(int i=c-b;i<=d-a;i++){
        arr.push_back(2*());
    }
    return 0;
}